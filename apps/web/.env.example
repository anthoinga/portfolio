# Ignore Build Step: node scripts/ignore-build.mjs
#
# After creating a Sanity project:
# 1. Set PUBLIC_SANITY_* and SANITY_API_READ_TOKEN on Vercel (token = Viewer).
# 2. sanity deploy from apps/studio.
# 3. Sanity webhook (project + siteSettings, published) → Vercel Deploy Hook.
